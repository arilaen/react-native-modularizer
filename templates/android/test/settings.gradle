// Uncoment when running the app directly
rootProject.name = "TestConsumerApp"
include ':app'

include ':{{{moduleName}}}'
project(':{{{moduleName}}}').projectDir = new File(rootProject.projectDir, './{{{moduleName}}}')
{{#settings}}
  {{{.}}}
{{/settings}}
